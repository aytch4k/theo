version: '3.8'

services:
  # SQLite doesn't need a container as it's embedded

  # Aerospike
  aerospike:
    image: aerospike/aerospike-server:latest
    ports:
      - "3000:3000"
      - "3001:3001"
      - "3002:3002"
    volumes:
      - aerospike-data:/opt/aerospike/data
    environment:
      - NAMESPACE=theo
    healthcheck:
      test: ["CMD", "asinfo", "-v", "status"]
      interval: 10s
      timeout: 5s
      retries: 3

  # IPFS
  ipfs:
    image: ipfs/kubo:latest
    ports:
      - "4001:4001"
      - "5001:5001"
      - "8080:8080"
    volumes:
      - ipfs-data:/data/ipfs
    environment:
      - IPFS_PROFILE=server
    healthcheck:
      test: ["CMD", "ipfs", "id"]
      interval: 10s
      timeout: 5s
      retries: 3

  # IndexedDB doesn't need a container as it's browser-based
  # But we can add a simple web server to serve test pages
  indexeddb-test:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - ./test/indexeddb:/usr/share/nginx/html
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Theo verification service
  theo-verify:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - aerospike
      - ipfs
    volumes:
      - ./:/app
    environment:
      - AEROSPIKE_HOST=aerospike
      - AEROSPIKE_PORT=3000
      - AEROSPIKE_NAMESPACE=theo
      - IPFS_HOST=ipfs
      - IPFS_PORT=5001
      - IPFS_PROTOCOL=http
    command: ["go", "run", "/app/scripts/verify-storage.go"]

volumes:
  aerospike-data:
  ipfs-data: